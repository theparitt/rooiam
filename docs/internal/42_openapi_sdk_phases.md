# OpenAPI + SDK — Phased Plan

Status: **In progress — Phases A–D done (2026-06-02). Next: Phase E (refactor consumers) or Phase D backfill (OIDC/PKCE, webauthn, mfa).**

Goal: a typed, language-agnostic SDK layer so downstream apps integrate with
Rooiam through generated clients, not hand-rolled `fetch`. The server's OpenAPI
spec (generated by `utoipa`, so it cannot drift) is the single source of truth.

Background design: [41_sdk_plan.md](./41_sdk_plan.md) (two TS packages, monorepo,
generate-from-code). This doc is the **execution plan** with phases.

Hard rule (locked): **the SDK must be 100% stable + tested against the live
server before any working frontend (rooiam-admin, rooiam-app) is refactored onto
it.** Prove the layer first; move the apps last.

---

## Phase A — OpenAPI foundation on the server ✅ (done)

Prove the `utoipa` pipeline end-to-end with one real endpoint.

- [x] Add `utoipa` + `utoipa-swagger-ui` to `rooiam-server/Cargo.toml`
- [x] Create `src/openapi.rs` (`ApiDoc` aggregator)
- [x] Annotate first endpoint: `GET /v1/orgs/integrations/workspace`
      (`#[utoipa::path]` + `#[derive(ToSchema)]` on its response)
- [x] Serve `/openapi.json` + Swagger UI at `/docs` (in `main.rs`)
- [x] Verify the generated spec is valid OpenAPI 3.1.0

Result: the server emits a correct, code-generated spec. Foundation works.

---

## Phase B — Annotate the integration surface (server) ✅ (done)

Covered the **entire** `/v1/orgs/integrations/*` workspace API-key surface:
**24 path entries / 33 operations** in the generated spec, all under the
`integrations` tag with the `workspace_api_key` bearer security scheme registered
(via a `Modify` impl in `openapi.rs`).

- [x] workspace
- [x] members: list, detail, activity, profile (PATCH), sessions (GET+DELETE), role (PATCH), remove (DELETE)
- [x] activity (workspace audit log), audit/actions
- [x] clients: list, create, detail, update, delete, status, secret-metadata, rotate-secret
- [x] invites: list, send, detail, revoke
- [x] branding (GET+PATCH), auth-config (GET+PATCH), effective-policy,
      policy-summary, roles, permissions, widget-preview-config, api-keys/me

Six request-body DTOs derive `ToSchema` and appear in `components.schemas`:
`UpdateCurrentOrganizationBrandingRequest`, `UpdateTenantAuthConfigRequest`,
`CreateOrgClientRequest`, `UpdateOrgClientRequest`, `UpdateOrgClientStatusRequest`,
`SendInviteRequest` (plus the two member DTOs from the core slice). Response
bodies remain loose (`status` + `description`) — the path/params/auth are what the
SDK needs; tighten to `body = X` later if useful.

**Live contract finding:** an invalid/missing API key returns **400**
(`AppError::Validation`), not 401 — the SDK test was corrected to assert a 4xx
`RooiamError`. The 21 handlers were made `pub` so utoipa's generated `__path_*`
items are reachable from `openapi.rs` (`InvitePath` too, once its handlers went pub).

### The annotation recipe (mechanical — repeat per endpoint)

The 30 remaining endpoints are pure repetition of three proven shapes. For each:

1. Make the handler **`pub`** (utoipa's generated `__path_*` item must be
   reachable from `openapi.rs`).
2. Add the `#[utoipa::path(...)]` attribute above the handler.
3. Register the handler in `src/openapi.rs` under `paths(...)`.
4. (If it has a named response struct, derive `utoipa::ToSchema` on it and add it
   to `components(schemas(...))`.)

**Shape 1 — no params** (e.g. `workspace`, `roles`, `permissions`):
```rust
#[utoipa::path(get, path = "/v1/orgs/integrations/roles", tag = "integrations",
    security(("workspace_api_key" = [])),
    responses((status = 200, description = "..."), (status = 401), (status = 403)))]
```

**Shape 2 — query params** (e.g. `members`, `activity`, `clients` list): add
`#[derive(utoipa::IntoParams)]` + `#[into_params(parameter_in = Query)]` to the
query struct, then:
```rust
#[utoipa::path(get, path = "...", tag = "integrations",
    params(SomeListQuery), security(("workspace_api_key" = [])),
    responses((status = 200, description = "...")))]
```

**Shape 3 — path param** (e.g. `members/{member_id}`):
```rust
#[utoipa::path(get, path = "/v1/orgs/integrations/members/{member_id}", tag = "integrations",
    params(("member_id" = Uuid, Path, description = "Member UUID")),
    security(("workspace_api_key" = [])),
    responses((status = 200), (status = 404)))]
```

**Body shape — POST/PATCH** (e.g. create client, update role): derive
`utoipa::ToSchema` on the request body struct, add `request_body = TheStruct`:
```rust
#[utoipa::path(patch, path = ".../{member_id}/role", tag = "integrations",
    params(("member_id" = Uuid, Path)), request_body = UpdateMemberRoleRequest, ...)]
```

Response schemas can stay loose (just `status` + `description`) initially and be
tightened to `body = SomeResponse` per endpoint later — the path/params/auth are
what the SDK needs first.

Exit test: `/openapi.json` lists every `/orgs/integrations/*` path with correct
params (respecting `deny_unknown_fields`, `page_size` ≤ 1000); Swagger UI renders
them. Dump the spec without booting the DB via a throwaway example:
`examples/dump_openapi.rs` → `ApiDoc::openapi().to_pretty_json()`.

---

## Phase C — `@rooiam/sdk-server` (TypeScript, generated + wrapped) ✅ (done)

- [x] Scaffold the monorepo: `rooiam-sdk/packages/js-server` (`@rooiam/sdk-server`)
- [x] Generate base types from `/openapi.json` (`openapi-typescript` →
      `src/generated/schema.ts`; `npm run gen` regenerates)
- [x] Ergonomic wrapper covering the full surface: `workspace()`, `activity()`,
      `auditActions/effectivePolicy/policySummary/roles/permissions/apiKeyMe/widgetPreviewConfig()`,
      `members.{list,get,activity,sessions,revokeSessions,setRole,updateProfile,remove}`,
      `clients.{list,get,create,update,delete,setStatus,secretMetadata,rotateSecret}`,
      `branding.{get,update}`, `authConfig.{get,update}`, `invites.{list,get,send,revoke}`
- [x] API-key Bearer auth, base-URL config, `RooiamError` (status + parsed body),
      query-param helper that omits undefined

**Build note:** generated types must live in `schema.ts` (NOT `schema.d.ts`) —
`tsc` treats `.d.ts` as ambient and won't emit it to `dist`, which would break
`export type { paths } from './generated/schema.js'` for consumers. The `gen`
script outputs `schema.ts`.

Exit test (the stability bar) — **CLEARED**: the suite runs against a **live,
source-run rooiam-server** (port 5180, demo DB) with a real minted workspace API
key. **31/31 tests pass**, including a probe that hits all 11 read endpoints.
Without live env vars the live block skips and 26 unit tests pass. The SDK is
stable + tested against the real server — the bar for refactoring consumers is met.

---

## Phase D — `@rooiam/sdk-browser` (TypeScript) ✅ (core slice done 2026-06-02)

Cookie-based, NO secrets. Every request uses `credentials: 'include'`; auth is the
opaque `rooiam_session` cookie. A new `browser` tag + `session_cookie` (apiKey in
cookie) security scheme were added to the spec; `workspace_api_key` and
`session_cookie` now coexist. Spec is up to **40 operations** total.

Server endpoints annotated (7, all `pub`):
- [x] `GET /setup/auth-methods`, `GET /setup/login-bootstrap` (public; `PublicAuthMethodsQuery` got `IntoParams`)
- [x] `POST /auth/magic-link/start`, `POST /auth/magic-link/verify` (public; sets session cookie)
- [x] `POST /auth/logout`, `GET /identity/me`, `PATCH /identity/me/profile` (session cookie)
- DTOs deriving `ToSchema`: `StartMagicLinkRequest`, `VerifyMagicLinkRequest`, `UpdateProfileRequest`

SDK (`@rooiam/sdk-browser`, `RooiamBrowser`):
- [x] Public flow: `authMethods()`, `loginBootstrap()`, `startLogin()`, `verifyLogin()`
- [x] Session self-service: `me()`, `updateProfile()`, `logout()`
- [x] `RooiamError` (status + body); `credentials: 'include'` baked in; no Authorization header ever
- [x] Follows the widget redirect contract ([[project_rooiam_widget_redirect_contract]])

Exit test (stability bar) — **CLEARED**: live-tested against the source-run server.
**16/16 pass** (13 unit + 3 live). Live block exercises the public endpoints
(`auth-methods`/`login-bootstrap` → 200, `me()` → 401 with no cookie). Session-cookie
calls aren't live-tested headlessly (would need a minted login cookie) — covered by unit tests.

Deferred to a later Phase-D pass (not in the core slice): OIDC authorize/token/userinfo
+ PKCE helpers, webauthn/passkeys, mfa/totp, the rest of `/identity/me/*`
(sessions, linked-accounts, email-change, delete, audit-logs). The pipeline +
cookie-auth + the stability bar are proven; these are mechanical backfill.

### Phase D backfill #1 — OIDC client flow + PKCE ✅ (done 2026-06-02)

The standard OIDC login flow downstream apps use. New `oidc` tag +
`oidc_access_token` bearer scheme. Spec is up to **45 operations**.

Server endpoints annotated (5, already `pub`):
- [x] `GET /.well-known/openid-configuration` (discovery), `GET /.well-known/jwks.json`
      — these live at the ORIGIN root, **not** under `/v1`
- [x] `GET /v1/oidc/authorize` (`AuthorizeRequest` → `IntoParams`; 302 redirect)
- [x] `POST /v1/oidc/token` (`TokenRequest` → `ToSchema`; **form-encoded**, so
      `request_body(content = TokenRequest, content_type = "application/x-www-form-urlencoded")`)
- [x] `GET /v1/oidc/userinfo` (`oidc_access_token` bearer)

SDK additions (`@rooiam/sdk-browser`):
- [x] PKCE helpers (Web Crypto, exported standalone): `generateCodeVerifier()`,
      `deriveCodeChallenge()`, `createPkcePair()` — S256, verified against the
      RFC 7636 Appendix B test vector
- [x] `oidc.{discovery, jwks, authorizeUrl, exchangeCode, userinfo}` —
      `authorizeUrl()` is pure string-building; `exchangeCode()` sends
      `application/x-www-form-urlencoded` with the verifier and NO client secret
      (public client); `userinfo()` sends the access token as a Bearer header

**Live contract finding #2:** discovery + JWKS are served at the **origin root**
(`/.well-known/*`), and `/v1/.well-known/*` is a 404. The SDK's normal `request()`
prepends `apiBase` (which includes `/v1`), so those two methods pass an explicit
`base: this.origin` override. Caught by the live test before shipping.

Exit test — **CLEARED**: **25/25 pass** (20 unit + 5 live). Live block exercises
discovery + jwks against the source-run server; a manual probe confirmed
`POST /oidc/token` parses the form body (returns an OAuth error, not a 415).

---

## Phase E — Refactor consumers onto the proven SDK (one at a time)

Only after C and D pass their stability bars.

- [ ] candycloud-server / candycloud-web (first — already hand-roll it)
- [ ] rooiam-admin (replace hand-rolled `api.ts`)
- [ ] rooiam-app (replace hand-rolled fetch calls)

Each refactor is verified before the next. Payoff: the contract bugs that came
from hand-rolled fetch (oversized `page_size`, unknown-field rejections, wrong
redirect params) become impossible — the SDK types ARE the server's contract.

---

## Phase F — Other languages + mobile (generated from the same spec)

Once the spec is complete and the TS SDKs are proven:

- [ ] Author/finish the full `openapi.yaml` coverage (annotate remaining
      endpoints beyond integrations)
- [ ] Generate server SDKs for the languages `rooiam-examples` use
      (`openapi-generator`)
- [ ] Mobile auth client (OIDC + PKCE + device-code) that `rooiam-android`
      consumes — feeds the API-login + device-login roadmap items
      ([40_device_login_plan.md](./40_device_login_plan.md),
      [release_roadmap.md](./release_roadmap.md))

---

## How this maps to product phases

This work is the technical foundation for **Phase 7 — First External Integration
Win** in [product_phases.md](./product_phases.md): a clean, typed, generated SDK
is what makes "an outside team integrates Rooiam quickly" real. Phases A–E here
sit under that product phase; Phase F extends into the API-login lane.
